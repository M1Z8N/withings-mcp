[project]
name = "withings-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "M1Z8N", email = "mizantompkins@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
withings-mcp = "withings_mcp:main"

[build-system]
requires = ["uv_build>=0.11.32,<0.12.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "ruff>=0.16.0",
]
